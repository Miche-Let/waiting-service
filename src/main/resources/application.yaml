spring:
  application:
    name: waiting-service

server:
  port: 19200

eureka:
  client:
    enabled: false
#    service-url:
#      defaultZone: ${EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka/}
